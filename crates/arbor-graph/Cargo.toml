[package]
name = "arbor-graph"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
readme.workspace = true
description = "Graph schema and relationship tracking for Arbor"

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
tracing.workspace = true
sled = "0.34"
bincode = "1.3"

arbor-core = { path = "../arbor-core", version = "3.0.0" }
petgraph = { version = "0.6", features = ["serde-1"] }
tiktoken-rs = "0.5"
once_cell = "1.19"

# Only for `kill(pid, 0)` liveness checks on a detached SCIP worker. Already in
# the tree transitively; a subprocess-based `kill -0` would be the alternative.
[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
criterion = "0.5"
tempfile = "3.0"

[[bench]]
name = "graph_bench"
harness = false
